# import random
#
# #Random Name Picker
# import random
# names_string = input("Give me everybody's names separated by a comma: ")
# names = names_string.split(",")
# num_items = (len(names))
# myRandomNames = random.randint(0, num_items - 1)
# person = names[myRandomNames]
# print(f"Person who will play is {person}")
# #print(random.choice(toSplit))